import React, { Component } from "react";
import { Link } from "react-router-dom";

class NavBar extends Component {
  render() {
    return (
      <nav className="navbar navbar-expands-sm navbar-dark px-sm-5">
        <p>Nav bar</p>
      </nav>
    );
  }
}

export default NavBar;
